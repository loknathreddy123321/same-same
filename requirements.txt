# Core agent
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.0

# AWS
boto3>=1.34.130      # bedrock-runtime Converse API needs 1.34.116+
botocore>=1.34.130
tenacity>=8.2.0

# LLM
groq>=0.9.0
openai>=1.40.0        # AzureOpenAI client

# YAML parsing
pyyaml>=6.0.1
ruamel.yaml>=0.18.0   # round-trip mode: preserves comments, key order, quote style

# Dashboard / API
fastapi>=0.111.0
uvicorn>=0.29.0
python-dotenv>=1.0.1
pydantic>=2.0.0

# Dev / testing
pytest>=8.0.0
moto[all]>=5.0.0
